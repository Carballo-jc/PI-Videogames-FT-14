// import { useSelector } from "react-redux";

// export const getFromDB = (id) => {
//   const allGamers = useSelector((state) => state.videogames);

//   return allGamers.filter((gamer) => gamer.id === id);
// };
